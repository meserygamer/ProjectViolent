<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="TabMenuGrid"
           TargetType="Grid">
        <Setter Property="Background" Value="#03bfff"></Setter>
        <Setter Property="Grid.Row" Value="1"></Setter>
    </Style>
    <Style x:Key="UsersListButton" TargetType="Button">
        <Setter Property="Grid.Column" Value="0"></Setter>
        <Setter Property="Template" Value="{StaticResource TabMenuButton}"/>
    </Style>
    <ControlTemplate x:Key="TabMenuButton" TargetType="Button">
        <Border BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                Background="{TemplateBinding Background}">
            <ContentPresenter x:Name="contentPresenter"
                              Focusable="False"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              Margin="{TemplateBinding Padding}" RecognizesAccessKey="True"
                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
        </Border>
    </ControlTemplate>
</ResourceDictionary>